{
  "name": "@flagent/design-system",
  "version": "0.1.0",
  "private": true,
  "description": "Flagent design tokens — single source of truth for CSS, TS, Kotlin, Swift",
  "scripts": {
    "build": "node build-tokens.js",
    "generate": "node build-tokens.js"
  }
}
