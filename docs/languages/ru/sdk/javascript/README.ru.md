# JavaScript/TypeScript SDK (base, generated)

Это base TypeScript/JavaScript SDK для Flagent API, **сгенерированное из OpenAPI** (использует `axios`).

Если вам нужен более удобный API (кэширование, удобные хелперы) — используйте `javascript-enhanced/`.

## Сборка

```bash
npm install
npm run build
```

## Установка

Версию см. в корневом [VERSION](https://github.com/MaxLuxs/Flagent/blob/main/VERSION) или [Releases](https://github.com/MaxLuxs/Flagent/releases).

Если пакет опубликован:

```bash
npm install @flagent/client@0.1.6 --save
```

Локально (не рекомендуется для production):

```bash
npm install PATH_TO_GENERATED_PACKAGE --save
```

## Документация

Сгенерированная документация по endpoints и моделям находится в `docs/`.

Полная (англ.) документация генератора — в [`README.md`](./README.md).

